# Documentation index

**Canonical manifest**: use the repository root [`llms.txt`](../llms.txt) — it is the single source of truth for AI discovery paths.

This file exists so tools that only scan `docs/` still find a pointer upward.
